### Added
- Project mark (`docs/logo.png`) and a GitHub social-preview image
  (`docs/og-image.png`), plus `docs/icon.py`, the script that renders them.
- README now uses the centred header block with the mark, matching the
  house style rather than the plain-heading fallback.
